import { bY as gestureAnimations, H as animations, I as createDomVisualElement } from './gestures-CkwOd1OX.js';

/**
 * @public
 */
const domAnimation = {
    renderer: createDomVisualElement,
    ...animations,
    ...gestureAnimations,
};

export { domAnimation as d };
